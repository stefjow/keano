{
  "name": "lufterl",
  "private": true,
  "description": "runs the vendored lufterl-map smoke test (scripts/09_smoketest.js) as the pipeline's viz gate",
  "devDependencies": {
    "puppeteer-core": "^24.0.0"
  }
}
